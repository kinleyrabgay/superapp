import 'package:flutter/material.dart';

class ProfileWithLogin extends StatelessWidget {
  const ProfileWithLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
